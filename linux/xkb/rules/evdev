! include %S/evdev

! layout = types
  frenchy = +frenchy(leddigits)

! layout[1] = types
  frenchy = +frenchy(leddigits)

! layout[2] = types
  frenchy = +frenchy(leddigits)

! layout[3] = types
  frenchy = +frenchy(leddigits)

! layout[4] = types
  frenchy = +frenchy(leddigits)

! layout = compat
  frenchy = +frenchy(leddigits)

! layout[1] = compat
  frenchy = +frenchy(leddigits)

! layout[2] = compat
  frenchy = +frenchy(leddigits)

! layout[3] = compat
  frenchy = +frenchy(leddigits)

! layout[4] = compat
  frenchy = +frenchy(leddigits)

! layout option = symbols
  * shift:frenchy_capslock = +frenchy(shiftlock)

! layout[1] option = symbols
  * shift:frenchy_capslock = +frenchy(shiftlock):1

! layout[2] option = symbols
  * shift:frenchy_capslock = +frenchy(shiftlock):2

! layout[3] option = symbols
  * shift:frenchy_capslock = +frenchy(shiftlock):3

! layout[4] option = symbols
  * shift:frenchy_capslock = +frenchy(shiftlock):4
