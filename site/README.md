# Site

Vide pour l'instant. Le site sera un site statique rails-static.

`bin/build` produit déjà `out/sheet.html` : un clavier complet, imprimable en A4, généré depuis `layout.yml`. C'est la page centrale du site, il restera à l'habiller et à écrire autour.
